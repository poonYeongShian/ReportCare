import 'package:flutter/rendering.dart';

class Radii {
  static const BorderRadiusGeometry k6pxRadius = BorderRadius.all(Radius.circular(6));
  static const BorderRadiusGeometry k25pxRadius = BorderRadius.all(Radius.circular(25));
}